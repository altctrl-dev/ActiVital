# Example environment variables for local development
# DO NOT store secrets in this file. Copy to .env and fill real values locally.

# Flask
FLASK_ENV=development
FLASK_APP=app.py

# Database: example Postgres URI
# Format: postgresql://<user>:<password>@<host>:<port>/<database>
DATABASE_URL=postgresql://youruser:yourpassword@localhost:5432/activital_dashboard

# Flask secret key (use a secure random value in production)
SECRET_KEY=replace-with-a-secure-secret

# Optional: custom logs directory
# LOGS_DIR=/path/to/logs
